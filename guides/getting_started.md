# Getting started

- Install [`habanero`](https://hex.pm/package/habanero)
- Add the route to your app & deploy it
- Set up the secret locally and on your running instance
- Set up the local watcher (including config)
- Make a change, see the hit deploy in action
